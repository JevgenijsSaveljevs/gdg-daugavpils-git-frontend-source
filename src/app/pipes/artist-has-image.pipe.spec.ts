import { ArtistHasImagePipe } from './artist-has-image.pipe';

describe('ArtistHasImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistHasImagePipe();
    expect(pipe).toBeTruthy();
  });
});
