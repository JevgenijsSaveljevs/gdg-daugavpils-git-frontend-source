import { TrackHasImagePipe } from './track-has-image.pipe';

describe('TrackHasImagePipe', () => {
  it('create an instance', () => {
    const pipe = new TrackHasImagePipe();
    expect(pipe).toBeTruthy();
  });
});
