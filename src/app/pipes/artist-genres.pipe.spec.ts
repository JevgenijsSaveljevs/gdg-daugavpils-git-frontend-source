import { ArtistGenresPipe } from './artist-genres.pipe';

describe('ArtistGenresPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistGenresPipe();
    expect(pipe).toBeTruthy();
  });
});
