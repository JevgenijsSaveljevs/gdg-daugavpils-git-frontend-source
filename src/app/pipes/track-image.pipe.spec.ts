import { TrackImagePipe } from './track-image.pipe';

describe('TrackImagePipe', () => {
  it('create an instance', () => {
    const pipe = new TrackImagePipe();
    expect(pipe).toBeTruthy();
  });
});
